# 2026-02-07 집에서 수정한 내용

- **삐뚤빼뚤 말투**: 반말 캐릭터 유지, 반말로만 통일 (해요체 혼용 제거). `blog/writers.js`, `blog/pipeline/humanizer.js`
- **포스팅 글자 수**: Ghost 기준 최소 1,500 words. `blog/pipeline/draftWriter.js` — LENGTH_RANGES를 words 기준으로 상향 (1500~2600)
- **미리보기 이미지**: 텔레그램으로 보낸 **첫 번째 사진**을 Ghost feature image(맨 위/미리보기)로 사용. `blog/agent.js`
- **로그 뷰어 빈 드롭다운**: `data/logs` 비어있어도 드롭다운에 로그 선택 가능하도록 기본 소스(scheduler.log, daily-restart.log, blog-agent.log) 표시. 배포 시 해당 로그 파일 touch. `log-viewer/server.js`, `.github/workflows/deploy.yml`
- **포스팅 비공개**: 안정화될 때까지 Ghost 발행 시 `status: 'draft'` (비공개). 안정화 후 `'published'`로 변경하면 됨. `blog/pipeline/publisher.js`
- **기타**: `blog/package-lock.json` 커밋 추가 (재현 가능 설치용)
